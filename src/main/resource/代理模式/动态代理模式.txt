一：动态代理模式

	定义：

	特点：
		a:
	优点：
		a:
	缺点：
	  	a：
	  	b:

	应用案例：

	应用场景：
		a:
		b:

JDK动态代理执行过程：
		a:获取被代理类的引用，并且得到被代理类的所有接口(通过反射实现)
		b:通过JDK的Proxy类生成一个新的类，并且实现它的所有接口的方法。
		c:动态生成Java文件，并且把增强的逻辑加入到新的代码中。
		d:编译生成Java文件的class文件
		e:加装并运行新的class文件，获得全新的Java类。


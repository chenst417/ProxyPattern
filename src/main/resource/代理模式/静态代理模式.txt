一：静态代理模式

	定义：

	特点：
		a:代理类和目标类需要实现同一个接口，而且目标类需要事先声明定义。
	优点：
		a:在不修改目标类的方法的前提下，实现功能的扩展。
	缺点：
	  	a：当修改接口的时候，代理类和目标类都需要维护。
	  	b:代理类和目标类实现的是同样的接口，当目标类很多时，
	  		需要实现的代理类也很多，代码不易管理和维护。不能动态扩展。
	应用案例：

	应用场景：
		a:
		b:
		c:
		d: